<view class="page">
  

  <!-- 主标题：专注/休息 -->
  <view class="main-title">
    <text>{{mode === 'work' ? '专注' : '休息'}}</text>
  </view>

  <!-- 大计时器 -->
  <view class="timer-container">
    <text class="timer">{{timerMinutes}}:{{timerSeconds}}</text>
    <text class="timer-state">{{timerStateText}}</text>
  </view>

  <!-- 番茄计数 -->
  <view class="tomato-count">
    <text>🍅 {{tomatoCount}}</text>
  </view>

  <!-- 工作时长 / 休息时长 选项 -->
  <!-- 工作时长选择区域 -->
<view class="option-section">
  <text>工作时长(分钟)</text>
  <view class="options">
    <block wx:for="{{workOptions}}" wx:key="index">
      <button 
        class="{{selectedWork === item ? 'active' : ''}}" 
        data-value="{{item}}" 
        bindtap="onSelectWork"
      >{{item}}</button>
    </block>
    <!-- 添加自定义按钮 -->
    <button bindtap="showWorkCustomInput">自定义</button>
  </view>
  
  <!-- 自定义输入框 (条件渲染) -->
  <view wx:if="{{showWorkCustom}}" class="custom-input">
    <input 
      type="number" 
      placeholder="输入分钟数" 
      value="{{customWorkValue}}"
      bindinput="onWorkCustomInput"
    />
    <button bindtap="confirmWorkCustom">确定</button>
    <button bindtap="cancelCustom">取消</button>
  </view>
</view>

<!-- 休息时长选择区域 (类似结构) -->
<view class="option-section">
  <text>休息时长(分钟)</text>
  <view class="options">
    <block wx:for="{{breakOptions}}" wx:key="index">
      <button 
        class="{{selectedBreak === item ? 'active' : ''}}" 
        data-value="{{item}}" 
        bindtap="onSelectBreak"
      >{{item}}</button>
    </block>
    <button bindtap="showBreakCustomInput">自定义</button>
  </view>
  
  <view wx:if="{{showBreakCustom}}" class="custom-input">
    <input 
      type="number" 
      placeholder="输入分钟数" 
      value="{{customBreakValue}}"
      bindinput="onBreakCustomInput"
    />
    <button bindtap="confirmBreakCustom">确定</button>
    <button bindtap="cancelCustom">取消</button>
  </view>
</view>


  <!-- 控制按钮 -->
  <view class="controls">
    <button class="control-btn start" bindtap="onStartPause">{{timerRunning ? '暂停' : '开始'}}</button>
    <button class="control-btn reset" bindtap="onReset">重置</button>
  </view>

</view>